package com.mash;

public class EndangeredAnimal extends Animal {
    private String health;
    private String age;


        // Additional fields for endangered animals
        // Add any other fields specific to endangered animals
    // Constructors
    public EndangeredAnimal() {
        // Call the constructor of the superclass (Animal) using super()
        super();
    }

    public EndangeredAnimal(int id, String name) {
        // Call the constructor of the superclass (Animal) using super()
        super(id, name);
    }

    // Additional methods for handling endangered animals
    // Add any other methods specific to endangered animals
}
